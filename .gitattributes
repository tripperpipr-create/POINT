# Концы строк в проекте смешанные: у каждого файла свои, и так и задумано.
# Часть файлов вдобавок хэшируется контрактами релиза — distribution
# хранит sha256 материалов сборки (.dockerignore, go.mod, Dockerfile.sandbox
# и другие). Любая нормализация меняет байты и роняет гейт в чистом клоне,
# поэтому Git не трогает содержимое ни при коммите, ни при выгрузке.
* -text
