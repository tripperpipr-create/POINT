# Point IDE — состояние проекта

Обновлено 13 сентября 2026 года, Point 1.2.2. Проект предназначен исключительно для личного использования. Критерий готовности — рабочие ежедневные сценарии, сохранность данных и согласованная локальная сборка.

**API-only (14 сентября).** Помощник и агенты принимают только HTTP-провайдеры
(Ollama / OpenAI-compatible / Anthropic / Azure). Claude Code, Codex и Cursor CLI
сняты из каталога и UI. Legacy-контур quest-proposals/flows в API сохранён, но
основной UI ведёт через WorkOrder v2 и Мастера.

## Внедрение Agent Hub

Реализованы версии задания и утверждения, precise/project, обсуждение, ограничение прав, достоверные критерии, checkpoint/resume и защита от повторного исполнения с неизвестным исходом. H01/H02 закрыты. Live 2×10 на `qwen3.5:9b` и поставка в portable/installed выполнены 7–8 сентября. С 9 сентября целевой контракт файлов — **как у Cursor**: агент пишет в открытый workspace; Change Set — журнал уже записанного и откат; Docker изолирует команды и сеть.

**URL intake (10 сентября):** контракты фаз 1–2/4–5 в дереве; PHP pack `1.3.1`; live PHP gate **0/2 PASS** (последний FAIL — stall на `propose_patch`). Требования к периодическому надзору Мастера и безопасной сети/git — [AGENT-HUB-ORCHESTRATOR-NETWORK-POLICY.md](AGENT-HUB-ORCHESTRATOR-NETWORK-POLICY.md). Сводка intake — [AGENT-HUB-URL-INTAKE-2026-09-10.md](AGENT-HUB-URL-INTAKE-2026-09-10.md). Общий статус внедрения — [AGENT-HUB-IMPLEMENTATION-2026-09-06.md](AGENT-HUB-IMPLEMENTATION-2026-09-06.md), модель — [agent-hub-mvp.md](agent-hub-mvp.md).

## Контур v2: наряд → доказательство → доставка (11–13 сентября)

Единицей работы стал **WorkOrder** — неизменяемая карточка запуска, которую Мастер собирает, а человек утверждает целиком: цель, scope и out-of-scope, критерии приёмки, вехи, план воркспейса (`existing|managed`, изоляция `git_worktree|snapshot|live_write`), ростер, политика маршрутизации моделей, сетевые гранты, секреты, бюджет и политика доставки. Модель может только предложить черновик; утверждение, изоляция и маршрутизация выводятся из доверенного локального состояния. Меняющееся состояние исполнения вынесено в производный `runtime` и в дайджест утверждения не входит, поэтому ход квеста не меняет утверждённый договор.

Завершение больше не следует из успеха Flow. Единственный терминальный путь утверждённого наряда проходит через шлюз доказательств: `EvidenceBundle` версии 3 обязан совпасть по дайджесту наряда и по дайджесту снимков источников, нести окружение, версионированный stack-preset и исполненную запись на каждую утверждённую проверку профиля завершения. Структурное расхождение даёт `blocked`, а не `completed`. Доставка подтверждается `DeliveryReceipt`; при `commitMode=squash` создаётся ровно один commit по явному списку изменённых файлов, без `git add -A`. При `applyMode=manual` проверенный результат остаётся в `isolated_review` и намеренно не трогает воркспейс.

Сопутствующее: неизменяемые снимки источников (текст, PNG/JPEG/WebP, файл, публичный HTTPS, git) с refresh и ограниченным diff требований; сертификация моделей и credential-free манифест возможностей подключения; долговременная writer-lease на воркспейс, переживающая паузу и перезапуск процесса; retention песочниц квеста (закрытие старше 30 дней при старте ядра); управление доставленным Docker Compose приложением с проверкой версии, дайджеста, расписки и ключа идемпотентности.

Контракт описан в [api.md](api.md) (18 эндпоинтов `/api/v2/*`), модель — в [agent-hub-mvp.md](agent-hub-mvp.md), поток — в [architecture.md](architecture.md). Legacy-контур (`/api/quest-proposals/decide`, `/api/change-sets/*`, `/api/workflows/*`, `/api/flows/*`) сохранён рядом как совместимость; v2-колбэк Flow не может уйти по старому пути завершения.

## Итог ревью и исправлений

Работают локальное Go-ядро, Code-OSS Agent Hub, инструменты, Skills, Quest/Flow, approvals, Change Set, SQLite history и диагностический Wails/Docker frontend. После аудита исправлены SQL read-only обход, потеря черновиков frontend, подготовка core перед проверками, устаревший источник навигационного checker и недоступный web workspace picker. UI/runtime/core пересобраны; локальные portable и installed файлы обновлены с резервной копией заменённых файлов.

Подробное исходное воспроизведение и таблица закрытия — [AUDIT-2026-09-05.md](AUDIT-2026-09-05.md). Новые функции инструментального слоя учитываются отдельно в [tool-access-layer.md](tool-access-layer.md).

## Разобран живой отказ запуска (14 сентября, ночь)

Первый живой прогон наряда встал: карточка навсегда показывала «Проверяем
окружение», кнопок, кроме «Отменить», не было, а созданного исполнителя человек
на карточке не видел. По журналу ядра и базе восстановлена вся цепочка:
утверждение в 22:19 упало на отсутствии Docker sandbox, три нажатия «Повторить
запуск» в 22:31 вернули наряд в `preflight`, после включения песочницы запуск
дошёл до планировщика milestone — и был оборван собственным таймаутом вебвью
(20 с) на 20-й секунде запроса к модели. Отменённый контекст унёс с собой и
запись отказа: квест остался в `preflight`, из которого нет выхода.

Закрыто:

1. **Запуск утверждённого наряда больше не живёт внутри HTTP-запроса.**
   `resumeApprovedWorkOrderV2` только начинает работу, а исход — `running`,
   `awaiting_user` или `blocked` с причиной — пишет фоновая задача со своим
   сроком (30 мин) и контекстом без отмены. Повторное нажатие не плодит вторую
   попытку: запуск один на квест, и «Отменить»/«Пауза» его останавливают
   (`internal/app/work_order_execution_v2.go`, `quest_controls_v2.go`).
2. **Карточка следит за живым квестом.** Пока её обновлял только ответ на
   нажатие, состояние замирало на снимке — независимо от того, что делал квест.
   Новый `vscode-extension/master-work-order-watch.js` опрашивает наряд, пока
   состояние переходное, и гаснет сам на исходе; обновление чужого разговора
   вебвью отбрасывает.
3. **«Будет создан» после утверждения стало «создан».** Исполнители создаются
   транзакцией утверждения, но карточка продолжала обещать их создание —
   человек искал создание агента, которого ядро уже создало. `WorkOrderRuntime`
   несёт `agentIds`, карточка сверяет ростер с ними.
4. **Включённый Docker sandbox перестал быть ловушкой.** Настройка глобальная, а
   ядро с `POINT_SANDBOX_BACKEND=docker` не стартует, пока Docker Engine не
   отвечает: IDE осталась бы без ядра во всех окнах. Теперь при таком отказе
   Point называет причину и предлагает выключить песочницу с перезапуском ядра.

## Проверено 18 сентября (вечер) — затворы, мёртвый код и дубли

Проход по техдолгу проверок и кодовой базы. Сквозной прогон без исключений:
`go build ./...`, `go vet ./...`, `go test ./... -count=1`, `check-docs`,
`check-quality-gate`, `check-release-contracts`, полный `npm run check`
расширения (62/62 смоука Хаба), сборка frontend. Race detector локально
по-прежнему не запускался: `CGO_ENABLED=0` и нет gcc в PATH.

**Затвор документации проверял чужое дерево.** `check-docs.mjs` брал состав
файлов обходом каталога, а в `.claude/worktrees` лежит worktree другой ветки —
вторая полная копия репозитория. Гейт видел 86 markdown-файлов при 43
отслеживаемых, и расходилось это в обе стороны: сломанная ссылка в чужой копии
роняла проверку локально и проходила в CI, а путь в бэктиках мог «найтись» в
копии, которой в чистом клоне нет. Состав теперь берётся у `git ls-files` — то
же, что видит чистый клон, и ничего сверх.

**CI перестал полагаться на кэш и научился называть класс отказа.** В job `go`
добавлены `-count=1`, явные сроки, замер покрытия без порога и отдельный шаг
детектора гонок: локально он невозможен, на ubuntu-раннере gcc есть. Сводка
прогона теперь ловит не только `--- FAIL`, но и `panic:`, `fatal error:` и
`test timed out` — отказы, которые роняют весь двоичный файл и снаружи
выглядят молчаливым «exit code 1», то есть ровно так, как выглядел плавающий
отказ.

**Смоук обязан быть кем-то вызван.** Список в `run-hub-smokes.mjs` намеренно
ручной, но обратной проверки не было, и файлы гнили молча — это случалось
дважды. `check-release-contracts` теперь требует, чтобы каждый `smoke-*` был в
раннере, в `npm run check` или в явном списке ручных проверок с причиной.
Упоминание в документе не считается: именно так выглядели все найденные
мертвецы. Затвор нашёл трёх. Два (`smoke-companion-brain-choice.js`,
`smoke-connection-form-local-cli.js`) проверяли CLI-исполнителей, снятых с
продукта 14 сентября, — удалены. Третий — ручная браузерная проверка на Edge и
Playwright; она в списке исключений, а её сегодняшний отказ записан в реестр
дефектов как `SMOKE_MASTER_SESSION_CONTROLS_STALE`, чтобы красная проверка не
считалась живой.

**Одна настоящая гонка в тестах.** В `internal/agent` тринадцать чтений карты
прогонов обёрнуты явным `repo.mu.Lock()`, а пять — нет; четыре из них сидят в
цикле ожидания рядом с живой горутиной движка. Добавлен `memoryRepo.run` под
тем же замком. Там же проверка активного бюджета перестала зависеть от
абсолютного числа: было `ActiveElapsedMs < 1000` после сна в 1200 мс, то есть
весь запас составляло время запуска, а проверяется «значение не выросло».
Пакет проходит 10× подряд.

Воспроизвести плавающий отказ не удалось: `internal/agent` и
`internal/providers` по 20 прогонов — зелено.

**Заведён линтер.** `staticcheck` на машине был старше Go 1.25 и не
запускался вовсе. После обновления — 58 находок, из них 30 по ST1005
(«строка ошибки не должна начинаться с заглавной») — все тридцать ложные: там
имена собственные (`Azure OpenAI…`, `Cursor runtime…`, `Point MCP…`) или русские
предложения. Правило снято в `staticcheck.conf` с названной причиной, а SA9003
(пустая ветвь `if`) добавлен сверх умолчания. Оставшиеся 28 закрыты, и
`staticcheck ./...` теперь зелёный и стоит в job `go` с закреплённой версией.

Что он нашёл, кроме мёртвого кода:

1. **Два теста, которые не проверяли ничего.** В `internal/connections` стоял `if` с
   пустым телом: условие вычислялось и выбрасывалось. В `internal/app` отпечаток
   пути сравнивался сам с собой. Оба заменены на утверждения, которые могут
   упасть; второй проверен отрицательно — при отключённой нормализации падает.
2. **Пустая ветвь в боевом коде** цикла учения — тот же почерк: условие есть,
   действия нет. Замечание сохранено комментарием, ветвь убрана.
3. **Отчёт приёмки MVP сообщал ноль вмешательств.** В `autoApprovePendingTools`
   передавалась карта для подсчёта, функция в неё ничего не писала, а вызывающий
   клал `len(карты)` в поле `Interventions`. Теперь функция возвращает число
   закрытых решений, и число в отчёте настоящее.
4. **Три мёртвых присваивания** (SA4006), nil-контекст и три упрощения.

Линтер для JS не заводился: шов, ради которого он был нужен — опечатка в имени на
стыке модулей вебвью, — уже закрыт `check-webview-exports.mjs`, а новая
зависимость ради остального не окупается.

**Снято мёртвого кода — 731 строка.** Из `internal/providers` убран провайдер
Claude Code CLI — сам файл, его тест и общий поиск headless-CLI:
конструктор вызывался только из собственных
тестов, а фабрика моделей про CLI не знает с 14 сентября. Константы
`ProviderCursor` / `ProviderClaudeCLI` / `ProviderCodexCLI` оставлены намеренно —
они нужны, чтобы старая запись отвергалась с явной ошибкой, а не принималась
молча. Удалены `agent.BuildToolRegistryWithServers` и `policy.EffectivePolicies`
— ноль вызовов и ноль тестов.

**Сведены дубли.** `utf8Prefix` жил двумя реализациями в `agent` и `workspace`,
`truncateUTF8` — двумя в `attachments` и `workflows`; все четыре
переизобретали `textutil.BoundedBytes`, который уже был в дереве и содержит
объединение их поведений. В вебвью два `shortName` расходились пределом — 24
руны против 28, и ни один текст об этом не говорил; правило вынесено в
`format-units.js` как `shortLabel(name, limit)`, оба предела сохранены на месте
вызова.

**Мелочи.** Удалён пустой `examples/go-health/.point-permission-1121381309` —
хвост диагностики прав, уехавший в коммит; шаблон внесён в `.gitignore`.
Бюджеты строк расширены на пять крупнейших неохваченных файлов, включая
`domain/hub.go` и `apply-overlay.mjs`: раньше файл попадал в список только
после того, как с ним уже помучились. Два работающих Electron-E2E
(`test-point-agent-context.ps1`, `test-point-guild-roster.ps1`) не вызывались
ниоткуда — внесены в релизный шаг и в текстовые контракты. Два
`verify-point-*.mjs` без парного `.ps1` названы в `docs/README.md`: найти их
можно было только обходом каталога. Цели `Makefile` приведены к job'ам CI —
прежний `make test` проверял заметно меньше затвора.

## Проверено 14 сентября (ночь) — сквозной прогон гейтов и четыре закрытых дефекта

Пройдено без исключений: `go build ./...`, `go vet ./...`, `go test ./...` (38 пакетов),
`check-docs`, `check-release-contracts`, `run-hub-smokes` 56/56, полный
`npm run check` расширения, сборка frontend (`npm test && tsc && vite build`),
`check-quality-gate`, `check-point-navigation --source-only`, `check-shell-tokens`,
`check-shell-scales`. Race detector по-прежнему не запускался: нет gcc в PATH.

Найдено и закрыто с регрессионными тестами:

1. **Выдуманный `proposalId` ронял ход Мастера.** Поле приходит от модели, и на
   пустом проекте слабая модель заполняет его наугад; ход падал с «задание для
   обсуждения не найдено в этом проекте», унося весь разбор. Теперь ссылка от
   модели, которая никуда не ведёт, игнорируется, а та же ссылка от клиента
   остаётся ошибкой: человек указал карточку, которой здесь нет
   (`internal/orchestrator/task_intake.go`).
2. **Гашение «размышления» уходило на строгий endpoint.** `chat_template_kwargs`
   — поле сверх спецификации OpenAI; официальный API отвечает на него 400. При
   этом аварийный повтор ставил признак до конца прогона, то есть один ход,
   потративший вывод на размышление, отравлял ошибкой формата все последующие
   запросы. Правило вынесено в `domain.RuntimeAcceptsThinkingSwitch` (свой
   рантайм по пресету либо нативная Ollama) и применяется всеми тремя
   вызывающими: движком агента, Мастером и помощником.
3. **Squash-доставка ломалась в подпапке репозитория.** `git status --porcelain`
   печатает пути от корня репозитория, а список доставки — от папки проекта;
   открытый монорепозиторий получал «изменение вне утверждённой доставки» на
   собственных файлах. Добавлена поправка на `rev-parse --show-prefix`
   (`internal/app/work_order_git_delivery_v2.go`).
4. **`point-db verify` мог назвать повреждённую базу здоровой.** Обход
   `PRAGMA foreign_key_check` не проверял `rows.Err()`, поэтому оборванное
   чтение выглядело как «нарушений нет» (`internal/storage/maintenance.go`).

Отдельно приведён в рабочее состояние харнесс живой приёмки МВП: он ни разу не
исполнялся и падал на собственной обвязке — не настроен Мастер, не открыт
проект, карточка с открытыми вопросами не утверждается. Теперь сценарий
доходит до исполнения, отвечает Мастеру на уточнения и умеет работать на
локальном рантайме (`POINT_V2_MVP_PROVIDER=ollama`, ключ не нужен). Живой
результат по-прежнему 0/2: на CPU-инференсе локальной 9B (замер — 6,3 токена/с)
сценарий Symfony + Compose + PostgreSQL не укладывается в бюджет наряда, и
прогон нужно вести на быстром endpoint'е.

## Проверено 14 сентября (вечер) — Hub v2 + API-only

- CLI-провайдеры (`claude-code-cli`, `codex-cli`, `cursor-cli`) убраны из каталога
  и отклоняются при сохранении подключений, помощника и агентов. Мозг помощника —
  только модель по API.
- UI: flows/workflows скрыты (редирект к Мастеру); legacy quest-proposal Start на
  обзоре/компаньоне заменён на «Обсудить с Мастером». Change sets / journal
  оставлены. WorkOrder v2 — основной путь запуска.
- `POINT_AGENT_HUB_V2=0` по-прежнему открывает legacy `workbench.db`.

## Проверено 14 сентября

Сокращение контура v2 до MVP — [AGENT-HUB-V2-MVP.md](AGENT-HUB-V2-MVP.md). Профиль завершения перестал быть списком имён: каждая проверка несёт команду и ожидаемый код возврата, входит в дайджест утверждения, исполняется на доставленной ревизии и требуется шлюзом. Расписка заявляет работающее приложение только после успешной проверки `service_start`; обещанное, но не поднявшееся приложение даёт `blocked` с сохранённым bundle, а не ошибку шлюза, стирающую доказательства. Перезапуск ядра переводит брошенные квесты v2 в `paused` вместо ложного `running`; заодно выяснилось, что headless-ядро не вызывало `Startup`, поэтому retention песочниц в продукте не запускался — вызов добавлен. Внешний конфликт воркспейса даёт `needs_review`. Probe накапливает прогоны матрицы вместо «1 из 9» при каждом запуске.

- `CGO_ENABLED=0 go build ./...` — PASS.
- `go test ./internal/...` — PASS, включая новые тесты профиля, восстановления после перезапуска и шлюза.
- `node scripts/check-docs.mjs`, `node scripts/check-release-contracts.mjs`, `node scripts/run-hub-smokes.mjs` — PASS.

**Переключение на Hub v2 (14 сентября).** По решению владельца ядро открывает `hub-v2.db` по умолчанию; прежняя `workbench.db` остаётся на диске нетронутой, и запуск с `POINT_AGENT_HUB_V2=0` снова открывает её. Новая база пустая: подключения, агенты и история прошлых квестов в ней не появятся, их нужно завести заново. Переключена база, а не набор экранов — legacy-поверхности интерфейса остаются на месте.

Не запускались: живой сценарий MVP, полный `npm run check` расширения, race detector, performance gate.

## Проверено 13 сентября

- `CGO_ENABLED=0 go build ./...` — PASS: дерево согласовано после волны v2.
- `go test ./internal/domain/... ./internal/storage/... ./internal/app/... ./internal/httpapi/...` — PASS (0.5 / 38.6 / 180.1 / 6.2 с).
- Сверка контракта: `node scripts/check-docs.mjs` — PASS; все 18 эндпоинтов `/api/v2/*` описаны в [api.md](api.md), расхождений нет.
- Состав Hub smoke — 56 сценариев в `scripts/run-hub-smokes.mjs` (было 32 на 5 сентября). Новый смоук `scripts/smoke-master-work-order-controls.mjs` зарегистрирован в списке и прогнан отдельно — PASS. Он проверяет карточку наряда по поведению: состав кнопок для `running`/`paused`, отсутствие рантайм-мутаций у терминального квеста и сквозной маршрут `controlMasterWorkOrderQuestV2` через webview → transport → extension host, включая получение ключа утверждённого маршрута из SecretStorage на `resume`.

- `node scripts/check-release-contracts.mjs` — PASS: 35 workflow-контрактов, 11 core и 11 desktop SLO.

Не запускались в этом проходе: полный `npm run check` расширения и остальные 55 Hub smoke, live PHP URL-intake, race detector, performance gate.

## Проверено 5 сентября

- Полный Go test без кеша и go vet — PASS.
- Полный extension npm run check — PASS, включая автоматическую сборку point-core/point-db, CSS/JS/runtime и 32/32 Hub smoke.
- Навигационный checker — PASS: 17 языковых профилей и 74 встроенные грамматики в локальной Code-OSS поставке. В обычном CI запускается явно обозначенная source-only часть; полный grammar check требует дерева Code-OSS.
- Frontend regression, TypeScript, Vite и отдельная Docker-сборка — PASS.
- SQLite regression и интеграция с временными PostgreSQL 17/MySQL 8.4 — PASS. Проверены и транзакции чтения, и ReadOnly-конфигурация соединения; CTE/EXPLAIN/function-in-SELECT не изменили данные. Подтверждённая запись работает.
- Новый VSIX собран; packaged runtime smoke — PASS. Перед упаковкой теперь автоматически собирается core.
- Installed renderer smoke — PASS: окно отвечает, один core, без runtime/authentication ошибок. UI E2E SQLite (CREATE/INSERT/SELECT/schema/test) и отображение отказа SSH — PASS. Исправлена гонка теста с отложенной отрисовкой результата.
- Четыре тестовые копии SQLite мигрировали через 1–37; source integrity сохранена. DR drill подтвердил exact backup/restore, recovery point и неизменность исходных фикстур.

Evidence миграций/DR лежит локально в build/audit-fix-migrations-20260905 и build/audit-fix-dr-20260905. Запись обновления локальных копий — build/local-update-20260905.json. Эти данные не публиковались.

## Карта исходников

Срез на 19 сентября, после разрезания вебвью и расширения: из
`ui/client/main.js` выделены клик-ветки помощника и первого запуска и весь
разговор с помощником, из `extension.js` — индекс проекта и консольные каналы.
Ядро с 18 сентября не менялось. Измерение исключает build, .cache, node_modules и другие генерируемые деревья. Строки считаются разделением по переводу строки, включая последний пустой элемент.

| Слой | Объём | Было 13 сентября |
| --- | ---: | ---: |
| internal | 35 пакетов, 391 production Go-файл | 33 пакета, 311 файлов |
| Go-тесты internal | 252 файла | 211 файлов |
| HTTP API | 201 маршрут, из них 19 `/api/v2/*` | 201 маршрут |
| Код v2 (`*_v2.go`, без тестов) | 40 файлов, ~6 600 строк | 46 файлов, ~5 600 строк |
| extension.js | 4732 строки | 6219 строк |
| ui/client/main.js | 5176 строк | 6818 строк |
| Модулей ui/client | 54 | 44 |
| internal/app/app.go | 666 строк | 748 строк |
| Команды / keybindings | 94 / 86 | 92 / 86 |
| scripts | 185 непосредственных файлов | 176 файлов |
| Схема SQLite | 67 миграций | 63 миграции |
| Смоуки Хаба | 63 сценария | 56 сценариев |

Рост числа файлов при том же объёме кода — результат разрезания: ни одного
production-файла длиннее 1100 строк не осталось. Крупнейшие были разложены по
семействам сценариев, имена взяты из уже принятой в пакетах конвенции:

| Было | Стало |
| --- | --- |
| `internal/app/orchestration.go` 2368 | 302 + семь файлов (`quest_start_from_proposal`, `flow_scheduler`, `flow_node_context`, `cursor_execution`, `flow_completion`, `revert_execution`, `flow_orchestrator_keys`) |
| `internal/agent/engine.go` 2290 | 334 + девять (`engine_loop`, `engine_start`, `tool_execution`, `engine_controls`, `engine_events`, `tool_plan`, `workspace_audit`, `system_message`, `tool_registry`) |
| `internal/app/hub.go` 1735 | 31 + шесть (`hub_bootstrap`, `agent_blueprints`, `hub_skills`, `hub_quests`, `hub_flows`, `hub_changesets`) |
| `internal/httpapi/server.go` 1733 | 247 + пятнадцать файлов маршрутов и `middleware`/`respond`/`events` |
| `internal/orchestrator/chat.go` 1615 | 535 + семь (`chat_types`, `chat_intent`, `chat_hire`, `chat_proposals`, `chat_text`, `chat_status`, `chat_store`) |
| `internal/storage/migrations.go` 1572 | 124 (реестр) + пять по областям схемы |
| `internal/workspace/index.go` 1563 | 167 + шесть (`index_build`, `index_context`, `index_search`, `index_tokens`, `index_drift`, `index_types`) |
| `internal/app/agent_learning.go` 1452 | 96 + пять шагов цикла учения |

Тесты разрезаны по тем же границам: `e2e_test.go` 1725 → 532,
`read_tools_test.go` 1435 → 475, `service_test.go` 1398 → 617. Самый длинный
тестовый файл теперь `master_chat_test.go` — 1317 строк.

В вебвью из `ui/client/main.js` выделены `decision-views.js` и
`companion-thread-views.js`, а 19 сентября — три обработчика:
`companion-actions.js` (36 клик-веток помощника), `onboarding-actions.js`
(32 ветки первого запуска и мастерской агента) и `companion-transport.js`,
который свёл в одно место отправку, поток и приём ответа помощника: раньше эта
тема жила двумя кусками на разных концах файла и порознь не читалась.

Разобран и слушатель входящих сообщений: из 863 строк и 80 веток в нём
осталось 488 и 54. Вынесены `master-inbox.js` (ответы Мастера — тринадцать
веток с одним `masterClient` и одной лентой), `hub-entity-inbox.js` (ядро
сохранило или удалило сущность Гильдии — одна форма на тринадцать ответов),
`run-inbox.js` (всё вокруг запуска: предпросмотры, старт, откат и разбор
отказа, который отпускает всё ждавшее) и `world-state-inbox.js` (полный снимок
мира — единственная ветка, заменяющая состояние целиком).

Из `extension.js` — `point-panels.js` и четыре обработчика однородных групп
сообщений: `roster-controller.js`, `learning-controller.js`,
`tooling-controller.js`, `cursor-controller.js`; `handleMessage` при этом
сократился с 838 строк до 503. 19 сентября добавились
`project-index-controller.js` (карта мира, её состояние в строке и сторож
изменений), `console-ssh-controller.js` (вкладки консоли, терминалы SSH,
«Найти в папке») и две темы из `BackendService`: `core-log.js` (буфер и
ротация хроники) и `core-lease.js` (дескриптор, замок запуска и аренды окон —
то, на чём держится общее тёплое ядро). Сам класс стал вдвое короче.

Переезды показали общую слабость проверок: те, что читали `extension.js` по
имени файла, при переносе функции либо падали, либо проходили вхолостую.
`ui/contracts.mjs` и `smoke-point-daily-workbench.mjs` переведены на чтение
всего пакета `vscode-extension/*.js` — тем же правилом, каким 18 сентября
перевели чтение пакетов Go.

Версии core/frontend/extension — 1.2.2; Code-OSS — 1.124.2. Node для разработки рекомендуется 24; CI выбирает major 24. Локальные проверки 5, 13 и 18 сентября выполнены на Node 22.11.0 и Go 1.25.0.

Что осталось

- **Живая приёмка v2**: шлюз доказательств закрыт тестами, но не подтверждён живым прогоном. Пока сборка `EvidenceBundle` не отлажена, корректно выполненная работа будет оседать в `blocked`.
- **Два контура рядом**: legacy quest-proposals / change-sets / workflows / flows сохранены как совместимость. Нужен явный статус адаптера, иначе расхождение путей завершения вернётся при первом рефакторинге.
- **Короткий путь для мелких задач**: наряд с одним критерием, без сетевых грантов и секретов. Иначе повседневная правка будет обходить собственный контур.
- **Master watch + сеть/git policy** (runtime v1): периодический надзор; confirmed git remotes; egress ask → пользователь. Осталось: DNS early-ask, EvidenceBundle fields, live PHP 2×.
- Live PHP URL-intake 2× PASS и фаза 6 ship URL-autonomy.
- Isolated filtered-copy — opt-in совместимость рядом с live-write.
- createHallOnboardingViews — 103 деструктурируемые зависимости (замер
  18 сентября по вызову в `ui/client/main.js`); фасад не сделан.
  Остальное из прежней строки «техдолг UI» закрыто 18 сентября: `handleMessage`
  сократился с 838 строк до 503, а запас бюджета `ui/client/main.js` вырос со
  129 строк до 504.
- **Плавающий тест в job `go`.** 18 сентября прогон CI №5 упал на шаге `go test
  ./...`, а прогон №6 на том же коммите прошёл; между зелёным №4 и красным №5
  Go не менялся вовсе — только JavaScript. Имя теста неизвестно: журналы
  Actions отдаются лишь после входа в аккаунт, а аннотация сообщает только
  «Process completed with exit code 1». Полный прогон на Linux в контейнере
  `golang:1.25` воспроизвести отказ не смог. Это не срабатывание
  десятиминутного предела: шаг `Test` в красном прогоне занял 113 секунд —
  ровно столько же, сколько в зелёных, то есть упал настоящий тест. Шаг `Test` теперь пишет имена
  упавших тестов в сводку прогона — следующий случай начнётся с имени, а не с
  воспроизведения вслепую. Плавающий затвор опаснее падающего: он врёт в обе
  стороны. **18 сентября (вечер):** воспроизвести не удалось — `internal/agent`
  и `internal/providers` по 20 прогонов зелёные. Сделано три вещи вокруг:
  сводка научилась ловить `panic:`, `fatal error:` и `test timed out` (у них
  нет ни одной строки `--- FAIL`, и снаружи они неотличимы от этого случая),
  добавлен `-count=1`, чтобы кэш не отвечал за незапущенный тест, и закрыта
  одна настоящая гонка в `internal/agent`. Детектор гонок теперь идёт
  отдельным шагом на ubuntu-раннере — локально он невозможен.
- Wails/Docker — диагностические клиенты; legacy profiles/workflows сохраняются осознанно.

## Локальная поставка и необязательный публичный выпуск

**Portable обновлён третий раз (14 сентября, 22:50).** Выложены исправления
живого отказа запуска: восемь файлов — `extension.js`, `master-chat-controller.js`,
`master-turn-stream.js`, новый `master-work-order-watch.js`, стагированный
`package.json`, `media/main.js`, `point-core.exe`, `point-db.exe`. Каждый сверен
по SHA-256 после копирования, заменённые лежат в
`.tmp/portable-ship-20260914-225045/portable-backup`, запись — в
`build/local-update-portable-ship.json`. Выложенный core проверен запуском:
`/api/health` отвечает `{"status":"ok","version":"1.2.2"}`. Перед выкладкой
остановлены два бесхозных `point-core.exe` (pid 52188 и 50844): окна Point не
было, аренд в `runtime` тоже. Установленная копия по-прежнему не тронута.

**Portable обновлён повторно (14 сентября, 22:30).** Второй заход добавил в
`.cache/VSCode-win32-x64` правки ленты Мастера и карточки запуска: пять файлов —
`extension.js`, `media/main.js`, `media/style.css`, `point-core.exe`,
`point-db.exe`, — каждый сверен по SHA-256 после копирования; заменённые лежат в
`.tmp/portable-ship-20260914-223011/portable-backup`. Выложенный core проверен
запуском. Установленная копия по-прежнему не тронута.

**Portable обновлён (14 сентября, 22:00).** В `.cache/VSCode-win32-x64` выложены
`point-core.exe` и `point-db.exe` сборки 21:59 с четырьмя ночными исправлениями,
`extension.js` и стагированный `package.json`; пять файлов, каждый сверен по
SHA-256 после копирования, заменённые сохранены в
`.tmp/portable-ship-20260914-220010/portable-backup`, запись — в
`build/local-update-portable-ship.json`. Установленная копия
`%LOCALAPPDATA%\Programs\Point` намеренно не тронута и остаётся прежней.
Выложенный core проверен запуском: `/api/health` отвечает `{"status":"ok"}`.
Перед выкладкой остановлено бесхозное `point-core.exe` (pid 51936, запущено в
20:26, простаивало): окна Point не было, аренд в `runtime` тоже, а файл был
занят.

**Копии разошлись (13 сентября).** Portable `.cache/VSCode-win32-x64` несёт контур v2: `extension.js` от 22:17, `media/main.js` от 22:41, `point-core.exe` от 22:42. Установленная копия `%LOCALAPPDATA%\Programs\Point` отстала — `extension.js` от 11 сентября, `main.js` и `point-core.exe` от 12 сентября, то есть наряда, шлюза доказательств и расписки доставки в ней нет. Проверять поведение v2 нужно в portable, либо сначала выложить сборку в установленную копию.

Последняя выкладка Hub (live-write + sandbox settings) — 9 сентября (`build/local-update-hub-ship.json`, backup `.tmp/hub-ship-20260909-*`). Сохранённый Windows installer от 2 сентября — исторический артефакт. Он не пересобирался. Его фактический SHA-256 и SBOM фиксирует build/code-oss/release.json; старые хэши из прежних отчётов не относятся к текущему installer.

Git-история заведена 18 сентября: 44 коммита на ветке `master`, remote `origin`. Production signing, formal field windows, 14-day dogfood и release-qualified 8h/24h soak не являются обязательным backlog для личного проекта. Существующие release workflow и validators сохранены для возможного будущего использования.

## Ограничения проверки

Race detector не запускался: CGO_ENABLED=0 и нет gcc в PATH. Не перепроверялись все реальные модели/провайдеры и внешние SSH. Длительный soak и новый сетевой CVE-аудит не проводились. Старый performance evidence от 1 сентября остаётся историческим измерением; изменения этого прохода не сопровождались новым полным performance gate.
