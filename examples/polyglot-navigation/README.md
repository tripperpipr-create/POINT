# Point polyglot navigation

Small files in several languages used to verify that Point keeps syntax support
language-agnostic and loads heavier language servers only when a user needs
them. `src/model.ts` also exercises Search Everywhere and Find Usages.
