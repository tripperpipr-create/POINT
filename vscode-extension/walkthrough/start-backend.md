# Локальный сервис

Point запускает встроенное локальное ядро `point-core` на свободном loopback-порту. История, профили и события остаются на вашем компьютере.

Статус локального ядра виден в строке состояния Point. Остановить или перезапустить его можно через палитру команд.
